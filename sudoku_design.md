# Chloe Sudoku Design & Technical Specification

## 1. 개요 (Objective)
Flutter를 사용하여 사용자가 표준 9x9 스도쿠 퍼즐을 즐길 수 있는 모던하고 직관적인 애플리케이션을 구현합니다. 난이도 선택, 실시간 점수(Score) 산출 및 툴팁 가이드, 메모(Note) 모드, 되돌리기(Undo), 완성된 숫자 패드 노란색 하이라이트, 직관적인 3단계 선 스타일링 및 상하좌우 셀 옅은 회색 강조 기능을 제공합니다.

---

## 2. 주요 기능 및 UI/UX 사양 (Key Features & UI/UX)

### 2.1 상단 컨트롤 & 정보 바
- **새 게임 버튼**: 앱바 상단 우측에 `+ 새 게임` 아웃라인 버튼 배치. 클릭 시 타이머 초기화 및 선택된 난이도의 새 퍼즐 시작.
- **난이도 선택 (Difficulty Selection - Blue Theme Matching)**:
  
  - 정보 바 좌측에 **연푸른색 팝업 카드 (`Colors.blue.shade50`)** 및 라운딩 디자인 (`16.0px`) 적용.
  - 선택 항목에 은은한 파란색 하이라이트 (`Colors.blue.shade100`) 및 파란색 텍스트/체크마크 아이콘 표시.
  - **초급 (Easy)**: 빈칸 30개
  - **중급 (Medium)**: 빈칸 42개
  - **고급 (Hard)**: 빈칸 52개
  - 난이도 변경 시 해당 난이도로 새 게임 자동 시작.
- **점수 및 규칙 툴팁 (Score & Rule Overlay)**:
  
  - 경과 시간에 따라 실시간 산출되어 표시 (`점수: X점` ⭐ 아이콘).
  - **점수 클릭**시 산출 규칙 표시되고, 다시 클릭 시 사라집니다.
  - **산출 규칙** (기본 점수 / 기준시간)
  
    초급 : 100점 / 180초 (3분)
  
    중급 : 300점 / 360초 (6분)
  
    고급 : 600점 / 600초 (10분)
  
    기준 시간 이내 성공하면 기본 점수 + (기준 시간 - 해결 시간)이 획득 점수
  
    기준 시간 이후 성공하면 기본 점수 - (해결 시간 - 기준 시간)이 획득 점수
  
    입력한 값이 올바르지 않을 때에만 점수가 -1점씩 반영 (그 이외에는 완료 및 종료 전에는 점수가 바뀌지 않는다)
    
    새로 고침 및 미완성된, 실패한 건은 0점 처리
  - 스토쿠를 성공하면 "축하합니다" 라는 메시지 팝업에 난이도, 점수, 경과 시간, 최종 점수가 계산되어 표시됨 (디자인은 버튼 색인 파란색 계열로 예쁜 팝업으로)
- **시간 측정 (Timer)**:
  
  - **경과 시간**: 1초 단위 실시간 스톱워치 (`mm:ss` / `hh:mm:ss`). 퍼즐 완료 시 타이머 멈춤 및 축하 팝업 출력.

### 2.2 메모(Note), 취소(Undo) 및 숫자 패드
- **메모 버튼**:
  - 메모 버튼을 먼저 누르면 메모 버튼이 **옅은 핑크색 배경 (`Color(0xFFFFEFF2)`)**으로 전환되고 빈 셀을 하나 클릭하면 메모 모드의 숫자가 입력된다.(메모 버튼 말고 빈셀은 핑크색으로 바뀌지 않음) 다른 빈 셀을 눌러도 메모를 다시 누르기 전까지 메모 모드로 유지된다.
     한 번 더 누르면 일반 모드로 토글 반전되고 입력되는 숫자는 일반 숫자가 입력된다(메모가 있었다면 모두 사라지고 입력한 일반 숫자가 입력만 입력됨)
  - 메모 상태에서 숫자 패드를 누르면 **9개가 지정된 위치에 작은 후보 숫자가 입력**:
    1. 좌측 상단 (`Top-Left`) 은 작은 후보 숫자 1
    2. 상단 가운데(`Top-Center`)은 작은 후보 숫자 2
    3. 우측 상단 (`Top-Right`) 은 작은 후보 숫자 3
    4. 중간 왼쪽 (`Center-Left`)은 작은 후보 숫자 4
    5. 중간 가운데 (`Center-Center`)은 작은 후보 숫자 5
    6. 중간 오른쪽 (`Center-Right`)은 작은 후보 숫자 6
    7. 좌측 하단 (`Bottom-Left`)은 작은 후보 숫자 7
    8. 우측 하단 (`Bottom-Center`)은 작은 후보 숫자 8
    9. 우측 하단 (`Bottom-Right`)은 작은 후보 숫자 9
  - 일반 상태일 때 숫자를 누르면 표준 대형 숫자 입력.
  - **자동 후보 삭제**: 다른 셀에 일반 정답 숫자가 들어오면, 같은 행/열/3x3 영역의 핑크색 칸 메모 목록에 포함된 해당 숫자가 **자동으로 지워짐**.
- **되돌리기 버튼**:
  - 숫자를 입력하거나 메모를 작성/삭제한 이전 동작 상태로 **한 단계씩 되돌리기(Undo)** 수행.
- **숫자 패드 버튼 완료 하이라이트**:
  - 스도쿠 전체 격자(9개 3x3 셀)에서 **특정 숫자(1~9 중)가 9개 모두 배치 완료**된 경우, 해당 **숫자 버튼의 배경색이 노란색(점수색, `Colors.amber.shade300`)**으로 변하여 완성을 직관적으로 알림.

### 2.3 스도쿠 격자 및 선 스타일 (Grid Line Hierarchy)
- **9×9 최외곽 프레임**: `2.0px` 두께의 검은색 (`Colors.black`) 외곽선.
- **3×3 블록 구분선**: `1.0px` 두께의 중간 회색 (`Colors.grey[600]`) 구분선으로 3x3 블록을 눈이 편하게 구분.
- **3×3 내부 셀 간의 선**: `0.5px` 두께의 연한 회색 (`Colors.grey[300]`) 미세선.
- 격자 바깥쪽 배경색은 옅은 핑크색

### 2.4 상호작용 및 시각적 피드백 (Visual Highlights)
- **선택 셀 강조**: 터치된 셀은 파란색 (`Colors.blue[200]`) 배경 강조.
- **상하좌우(행/열) 및 동일 숫자 옅은 회색 강조**:
  - 선택한 셀 기준 **가로칸 모두(행) 및 세로칸 모두(열)**를 **옅은 회색 (`Colors.grey[200]`)**으로 깔끔하게 강조.
  - 선택한 셀에 숫자가 있는 경우, 판 전체에서 **동일한 숫자가 들어간 셀들**도 옅은 회색 배경으로 함께 하이라이트.
- **실시간 유효성 검사**: 중복/규칙 위반 입력 시 숫자 빨간색 처리.

---

## 3. 검증된 개발 환경 및 빌드 사양 (Verified Build Environment)
- **Flutter SDK**: `3.47.2` (stable, `C:\sdk\flutter`)
- **Dart SDK**: `3.13.2`
- **Gradle**: `9.1.0` (`gradle-9.1.0-all.zip`)
- **Android Gradle Plugin (AGP)**: `9.0.1`
- **Kotlin**: `2.3.20`

---

## 4. 아키텍처 및 파일 구조 (Project Architecture)
- `lib/main.dart`: 앱 진입점 및 Material 3 테마 설정.
- `lib/models/sudoku_grid.dart`: 81개 셀의 메모 상태, 후보 숫자 리스트 및 복사(copy) 모델.
- `lib/logic/sudoku_engine.dart`: 난이도별 퍼즐 생성, 백트래킹 퍼즐 풀이 및 유효성 검증 엔진.
- `lib/widgets/sudoku_cell.dart`: 3단계 선 스타일링, 옅은 핑크색 메모 배경 및 6개 위치별 메모 숫자 렌더링 위젯.
- `lib/widgets/number_pad.dart`: 1~9 숫자 입력, 완성된 숫자 노란색 하이라이트 및 삭제 키패드 위젯.
- `lib/screens/home_screen.dart`: 메모 모드 토글, 자동 후보 삭제, completedNumbers 계산, Undo 히스토리 관리, 난이도 선택 및 메인 UI 화면.

---

## 5. 변경 이력 (Changelog)
- **v1.6.0** (2026-09-03):
  - **메모 위치 개편 (최대 6개)**: 좌측 상단, 우측 상단, 좌측 하단, 우측 하단, 중간 왼쪽, 중간 오른쪽 순서로 지정 배치 및 옅은 핑크색 배경(`Color(0xFFFFEFF2)`) 적용.
  - **완성된 숫자 버튼 노란색 하이라이트**: 스도쿠 판에 9개 모두 채워진 숫자의 버튼 배경을 노란색(`Colors.amber.shade300`)으로 변경하는 기능 적용.
